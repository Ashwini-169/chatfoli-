User Message → /api/chat → System Prompt + History → Gemini
→ Parse JSON & Plain Text → Return Both
→ Frontend: dispatch Redux Actions → Pre-fill Forms (editable)

"{
  "extractedData": {
    "section": "profile|workExperience|educations|skills",
    "fields": { "name": "John Doe", "email": "john@..." }
  },
  "nextQuestion": "...",
  "recruiterInsight": "..." (HR mode)
}
"